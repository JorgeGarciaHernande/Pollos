import 'package:flutter/material.dart';

class ManageItemsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gestionar Productos'),
      ),
      body: Center(
        child: Text('Vista para gestionar los productos'),
      ),
    );
  }
}
